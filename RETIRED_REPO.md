# Retired Repository

`/Users/jinjin/stocknews` was retired from production cron on 2026-07-06.

Current production repository:

```bash
cd /Users/jinjin/kiwoomnews
```

Use `kiwoomnews` for current operational questions:

- suspects and `data/radar_history/YYYYMMDD.json`
- `web/data/radar.json`
- autotrade state and stats
- `/tmp/kiwoom_*.log`

This repository may still be useful for historical reference, but it is not the source of truth after 2026-07-06.
