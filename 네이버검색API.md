
네이버 검색 API

AppName : stocknews

> 🔐 Client ID / Client Secret 은 평문으로 저장하지 않습니다.
> 값은 `.env` 파일에 보관합니다 (`.gitignore`로 추적 제외).
>
> - `NAVER_CLIENT_ID`
> - `NAVER_CLIENT_SECRET`
>
> 새 환경 설정 시 `.env.example`을 복사하여 `.env`로 만들고 값을 채우십시오.
